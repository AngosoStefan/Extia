{% extends "TBSBundle::layout.html.twig" %}

{% block subzone %}

	<h1>Passer une commande</h1>

	<form action="{{path("tbs_add_basket", { id: app.user.id}) }}" method="post" enctype="multipart/form-data">
		{{form_widget(form)}}
		<input type="submit" value = "Choisissez votre emplacement">
	</form>

{% endblock %}

