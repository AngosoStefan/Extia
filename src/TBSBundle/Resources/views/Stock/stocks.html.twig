{% extends "TBSBundle::layout.html.twig" %}

{% block subzone %}

	<h1>Tous les stocks</h1>

	<table>
		<tr>
			<th>Libell&eacute;</th>
			<th>Qtt totale</th>
		</tr>
		{% for stock in stocks %}
			<tr>
				<article>
					<header>
						<td>{{stock.SName}}</td>
						<td>{{stock.STotal}}</td>
						<td><a href="{{ path("tbs_edit_stock", { id: stock.SId}) }}"><span class="glyphicon glyphicon-pencil"></span></a></td>
						<td><a href="{{ path("tbs_delete_stock", { id: stock.SId}) }}"><span class="glyphicon glyphicon-remove"></span></a></td>
					</header>
				</article>
			</tr>
		{% endfor %}
	</table>

{% endblock %}